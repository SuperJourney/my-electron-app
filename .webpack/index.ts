import preloadConfig from "./preload.config"
import renderConfig from "./render.config"
import appConfig from "./app.config"
const config = [preloadConfig,renderConfig, appConfig]

export default config